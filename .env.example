# Used by docker-compose to advertise the Kafka broker address.
# Workers on other machines need to reach this IP:9092.
#
# Local dev (leader + workers on same machine): keep as localhost
# Cross-machine VPN setup: set this to your Tailscale IP (e.g. 100.x.x.x)
LEADER_IP=localhost
